# Face recognition app with Keras and Flask

### First, install pipenv
```bash
pip install pipenv
```

### Second, install dependencies using pipenv
```bash
pipenv install
```
### Finally, Run the main.py file
```bash
pipenv run python3 main.py
```
